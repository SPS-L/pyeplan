# Documentation-specific dependencies
# Core dependencies are installed from ../requirements.txt
setuptools
sphinx 
sphinx_rtd_theme
sphinxcontrib-bibtex<2.0.0

# Note: Core PyEPlan dependencies (numpy, scipy, matplotlib, etc.) 
# are installed from the root requirements.txt file
